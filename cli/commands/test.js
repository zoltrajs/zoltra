import { Logger } from "zoltra";

const logger = new Logger("DevCLI", undefined, false, true);

export const runTurboTest = () => {};

export const runTestTs = () => {};

export const runTest = () => {};
